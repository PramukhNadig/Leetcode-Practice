```Java
class Solution {
    public int[][] insert(int[][] intervals, int[] newInterval) {
        ArrayList<int[]> postI = new ArrayList<int[]>();
        int idx = 0;

                
        while(idx < intervals.length && intervals[idx][1] < newInterval[0]){
            postI.add(intervals[idx++]);
        }
        
        
        while(idx < intervals.length && newInterval[1] >= intervals[idx][0]){
            newInterval[0] = Math.min(newInterval[0], intervals[idx][0]);
            newInterval[1] = Math.max(newInterval[1], intervals[idx][1]);
            idx++;
        }
        postI.add(newInterval);
        
        
        while(idx < intervals.length){
            postI.add(intervals[idx++]);
        }
        

        
        return postI.toArray(new int[postI.size()][]);
    }
}
```
Runtime: 1 ms, faster than 98.79% of Java online submissions for Insert Interval.

Memory Usage: 48.2 MB, less than 18.23% of Java online submissions for Insert Interval.
